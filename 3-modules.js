// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum)
import names from './4-name'
import sayHi from './5-utils'
import data from './6-alternative-flavor'
import './7-mind-grenade'
// console.log(data);

// sayHi(`dung`);
// sayHi(names.john);
// sayHi(names.peter);
 